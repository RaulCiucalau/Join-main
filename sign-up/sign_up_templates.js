/**
 * Returns the HTML for the sign-up successful message.
 * @returns {string} HTML string for the sign-up success notification.
 */
function getSignUpSuccessHtml() {
  return `<div class="signup-successful-msg">
    <p>You Signed Up Successfully</p> 
  </div>`;
}
